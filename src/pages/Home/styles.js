import styled from "styled-components"

//estilizacao da pagina home

export const Styled = {
    CardWrapper: styled.div`
        display: grid;
        margin: 0 auto;
        width: 90%;
        height: auto;
        grid-template-columns: repeat(4, auto);
        justify-items: center;
    `
}